interface: auto
inbound:
  http: 9961
  firewall:
    kill-switch: true
dns:
  bootstrap:
    - 1.1.1.1
  nameserver:
    - doh, 1.1.1.1

proxy-group:
  Default:
    proxies:
      - DIRECT

rules:
  - FALLBACK, Default
